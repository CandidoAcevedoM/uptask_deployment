<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Inicia Session en UpTask

            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error                 

            form#formulario.caja.caja-login(method="POST" action`/iniciar-session `)
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" placeholder="Tu E-mail" value= email)
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Tu Password" value= password)
                .campo.enviar
                    input.boton(type="submit" value="Iniciar Session")
                .campo.acciones
                    a(href=`/crear-cuenta`) Crea una cuenta AQUI

                    a(href=`/restablecer-contrasena`) Olvidaste tu Contraseña?