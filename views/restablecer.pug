<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1= nombrepaginas 
            p Agrega tu E-mail para restablecer la Contraseña

            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
                                
            form.caja-login(action=`/restablecer-contrasena` method="POST")
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" placeholder="Tu E-mail" value= email)
                .campo.enviar
                    input.boton(type="submit" value="Restablecer Contraseña")
                .campo.acciones
                    a(href=`/iniciar-sesion`) Ir a iniciar session
                